# Generated by scripts/build_cerbos_policies.py; do not edit.
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: saas_channel
  rules:
  - actions:
    - execute
    effect: EFFECT_ALLOW
    roles:
    - user
    condition:
      match:
        all:
          of:
          - expr: request.principal.id == "user:maya@civic.example"
          - expr: request.resource.id in ["drive/create","gmail/send"]
          - expr: request.resource.attr.delegated_user == request.principal.id
          - expr: request.resource.attr.tool != "gmail/send" || request.resource.attr.purpose
              == "approved-communication"
