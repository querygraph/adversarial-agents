# Generated by scripts/build_cerbos_policies.py; do not edit.
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: delegation
  rules:
  - actions:
    - delegate
    effect: EFFECT_ALLOW
    roles:
    - user
    condition:
      match:
        all:
          of:
          - expr: request.principal.id == "user:maya@civic.example"
          - expr: request.resource.id == "report/public-summary"
          - expr: request.resource.attr.args.ttl <= 30
          - expr: request.resource.attr.args.scope == "report/public-summary"
