# Generated by scripts/build_cerbos_policies.py; do not edit.
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: credential
  rules:
  - actions:
    - read
    effect: EFFECT_ALLOW
    roles:
    - user
    condition:
      match:
        all:
          of:
          - expr: request.principal.id == "user:maya@civic.example"
          - expr: request.resource.id == "credential/northstar-study"
          - expr: request.resource.attr.args.raw == false
          - expr: request.resource.attr.args.scope == "northstar/study/*"
          - expr: request.resource.attr.args.ttl <= 60
