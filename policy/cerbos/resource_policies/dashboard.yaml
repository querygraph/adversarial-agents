# Generated by scripts/build_cerbos_policies.py; do not edit.
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: dashboard
  rules:
  - actions:
    - dataset:view
    effect: EFFECT_ALLOW
    roles:
    - user
    condition:
      match:
        expr: request.principal.id == "user:maya@civic.example" && request.resource.id
          == "dataset/northstar-study"
