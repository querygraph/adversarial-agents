# Generated by scripts/build_cerbos_policies.py; do not edit.
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: dataset
  rules:
  - actions:
    - read
    effect: EFFECT_ALLOW
    roles:
    - user
    condition:
      match:
        all:
          of:
          - expr: request.principal.id == "user:maya@civic.example"
          - expr: request.resource.id == "lakecat://northstar/household_energy"
          - expr: request.resource.attr.purpose == "energy-assistance-research"
          - expr: has(request.resource.attr.args.columns)
          - expr: size(request.resource.attr.args.columns) > 0
          - expr: request.resource.attr.args.columns.all(c, c in ["region","energy_source","monthly_energy_cost"])
          - expr: request.resource.attr.args.predicate == "cohort = 'approved'"
